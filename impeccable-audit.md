# Impeccable 前端检查与优化

日期：2026-09-09。预览：http://localhost:7777/。

## 结论

本轮在既有物忆界面上完成技术审查和定向优化，保留品牌、业务功能及无设备示例体验。实现一致性检查通过；已修复本轮确认的问题。以下评分是所检查范围的人工评价，不是 Lighthouse 得分或全站 WCAG 认证。

| 维度 | 评分 / 4 | 结果及边界 |
| --- | --- | --- |
| 可访问性 | 3 | 菜单/弹窗共用焦点管理、背景隔离和焦点返回；补齐模式选择与加载/错误语义。未进行真实读屏器全站验收 |
| 性能 | 3 | 首屏 CSS 减少 17.84%，缩略图避免无关重新请求，时间线后续图片延迟加载。未测真实低端手机的 Web Vitals |
| 响应布局 | 3 | 390 px 手机及 1280 px 桌面实页确认，菜单跨断点释放锁定，手机滑块与主要按钮加大触控区域 |
| 主题 | 3 | 修复设置选中态、说明文字、验收警告的深色对比度，统一焦点样式。高级页面未逐一覆盖所有硬件状态 |
| 实现一致性 | 4 | 复用现有色彩与组件，移除重复标题前缀和重复焦点逻辑，无新增运行依赖 |
| 合计 | **16 / 20** | Good：已完成本轮确认问题的修复，实机范围另行验证 |

## 已修复问题

| 级别 | 位置 | 证据与影响 | 修复 |
| --- | --- | --- | --- |
| P1 | AppShell 手机菜单 | 最后一个导航链接按 Tab 会离开菜单；遮罩后的内容仍可聚焦 | 与 Modal 共用 useOverlayFocus，隔离背景，补齐 dialog 语义、Tab 循环、Escape 和入口焦点返回；转桌面时取消锁定 |
| P1 | SettingsPage / PhysicalAcceptance.css | 深色主题仍使用固定浅色选中背景及固定深色警告文字，部分状态不易辨读 | 改用现有主题变量，增加设置说明字号和行高；保留语义徽章的独立颜色 |
| P2 | SettingsPage | 服务端等待值为 1.8 秒，原生滑块按 0.5 步进呈现为 2，与旁边说明不一致 | 步进改为 0.1，并提供独立名称；识别方式增加 aria-pressed |
| P2 | CamerasPage | Date.now() 出现在图片渲染路径，打开向导/编辑选项也会改变图片 URL | 图片版本仅随设备数据或定期健康结果更新，保留正常刷新；版本更新时可恢复先前失败的图片 |
| P2 | 首屏样式加载 | 约 21.7 KB 源码属于验收工具专用样式，却随每个普通页面加载 | 移至 PhysicalAcceptance.css，由两个现有验收页面按需导入；原有标记图像像素与规则保留 |
| P2 | 手机设置 | 说明与滑块并排导致滑动区域较短，部分按钮小于 44 px | 手机滑块放在说明下方；实测滑块为 236 × 44 CSS px；主要按钮、菜单摘要及图标按钮补齐 44 px 触控高度 |
| P2 | 共用弹窗 | Tab 候选可能包括折叠内容和负 tabindex 元素 | 焦点管理明确排除；背景原有 inert 状态在关闭后恢复 |

另外移除了 PageHeader 和设置分区重复的标题前缀，将关闭字符换为现有图标库图标，补齐安全区域内边距。减少动态效果偏好下仍保留可读的状态文字、静态进度与选中反馈。

## 性能测量

从修改前生产构建和初轮优化后的生产构建提取首页入口 CSS，使用同一 gzip 方法计算；后续审查修复的构建结果见文末：

| 指标 | 修改前 | 修改后 | 减少 |
| --- | ---: | ---: | ---: |
| 首屏 CSS 原始字节 | 103,613 | 85,132 | 17.84% |
| 同算法 gzip 字节 | 19,681 | 16,751 | 14.89% |

证据：`C:/Users/Administrator/Documents/ChatGPT/比赛/product-audit-20260909/impeccable/bundle-comparison.json`。这是首屏资源体积的减少，不代表全部路由的总代码减少或实测加载时间提升同等百分比。现有 3D 场景包仍有大于 500 KB 的构建提示，继续保持按需加载。

摄像头回归测试验证：打开来源向导、切换选项不改变已有缩略图 URL；关闭向导触发设备数据刷新后正常更新。事件列表前两张图片保持即时加载，后续截图使用浏览器延迟加载与异步解码。

## 实页与量测

证据目录：`C:/Users/Administrator/Documents/ChatGPT/比赛/product-audit-20260909/impeccable/`。

- 修改前：`before-settings-desktop.png`、`before-settings-mobile.png`、`before-menu-mobile.png`。
- 最终控件：`after-controls-desktop.png`、`after-controls-light.png`、`after-controls-mobile.png`。
- 最终交互：`after-menu-mobile.png`、`after-example-mobile.png`、`after-acceptance-desktop.png`。
- 桌面 1280 × 900、手机 390 × 844，DPR 1，原尺寸 PNG；所有列出的最终图片均已打开检查。
- 深色选中正文对比度 11.00:1，说明 5.94:1；浅色选中正文 13.54:1；深色验收警告 11.70:1。读数来自实际 DOM 的前景/背景颜色。
- 检查的手机页面无横向溢出。菜单 Tab 循环和 Escape 返回“更多”，以及示例关闭返回“先看示例”在实际浏览器确认。转回桌面后页面滚动和背景隔离恢复。
- 当前运行状态为 REAL，没有新增设备或物品，没有启用摄像头、扫描、麦克风，也没有保存设置。

## 有界验证记录

1. 先读取现有实现及修改前界面，再一次性完成核心修改。
2. 第一轮联合检查发现手机滑块仍偏短、验收警告文字对比度不足和刷新按钮挤压；集中修复后进行一次最终确认。
3. Impeccable 检测器运行一次，结果保存在 `detector.json`。两项发现分别为圆角面板的粗装饰边和进度条 width 过渡，均已移除。未反复运行检测器进行无界打磨。
4. 前端完整回归：37 个文件，390 项通过。修正新测试的不完整设置数据后，针对新增与首次使用的 16 项测试再次通过且没有该警告；最终生产构建通过，git diff --check 通过。
5. 浏览器日志只保留服务重启前 20:03:42 UTC 的旧 Vite WebSocket 连接失败记录，之后已重新载入当前代码，未发现新的应用错误。

## 保留与范围

临时浏览器尺寸、主题、减少动态效果模拟均已恢复；预览留在首页。本地服务按原 7777 前端、8018 后端和 REAL 模式重新启动，用户要求保留的无设备测试环境继续有效。本 PR 不包含本地临时代理配置或认证绕过。

真实设备接入成功率、照片识别准确性、持续视频流性能以及低端手机 Web Vitals 不在本轮零硬件验证范围。后续接入设备后应补充这些实机验证。

## PR 最终复核

- 修复后续代码审查发现的通知隔离问题：通知通过 portal 放入活动弹窗或菜单，保留背景 inert；嵌套弹窗关闭时返回上一层，关闭通知后焦点仍留在弹窗。
- 补齐手机伴侣的闪烁、波纹、摇晃及验收页加载动画的减少动态效果规则。浏览器模拟该偏好后，手机伴侣四个相关元素的 animationName 均为 none；模拟状态已恢复。
- 新增失败通知、键盘关闭及嵌套弹窗通知归位回归测试；浏览器确认活动弹窗中的通知区域无 inert 祖先。未进行真实读屏器验收。
- 同步主分支 `0260e4c`，保留 PR #1 合入时的独立审查修复及 PageBoundary 页面加载恢复。
- 同步后的完整前端测试：38 个文件、394 项通过；生产构建和差异格式检查通过。最终入口 CSS 为 85.27 kB（Vite 报告 gzip 16.88 kB），与上方初轮测量的构建及 gzip 口径区分。
- 截图和量测 JSON 路径为本机历史验收记录，未随 PR 上传；可复现回归测试位于 apps/web/tests。
