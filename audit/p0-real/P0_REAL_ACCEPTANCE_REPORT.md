> 公开历史文档副本：本机身份信息已脱敏，私人数据库与实拍媒体未公开。历史结果不代表当前版本通过验收；当前限制请见 README。

# P0 真实物品移动验收报告

## 当前结论

状态：`NOT_RUN`。

自动软件回归已完成，汇总状态为 `TOOLING_READY_PHYSICAL_NOT_RUN`。用户尚未执行物理手机移动，因此 P0 仍为 `NOT_RUN`，不是“真实闭环通过”。启动 REAL 验收页并完成摄像头预检后，进入唯一一次集中人工操作；实际启动信息见 `ready-for-physical.json`。

## 证据边界

- 本轮第三次正式长跑使用本机 OpenCV 索引 0，纯 soak 1,800.094 秒，30/30 门通过、正常退出并释放摄像头；物理设备身份仍须现场确认，不能当成真实手机移动。
- 当前物理试验数为 0，正确数为 0；准确率没有可计算样本。
- 没有 P0 before、after 或 movement clip 可列出。
- 没有真实 ESP32-CAM，未真机刷写。

## 自动回归与失败记录

Python 345/345、Vitest 58/58、真实 FastAPI/SQLite 浏览器 2/2、DEMO 文件视频浏览器 1/1、Mock 界面合同 6/6，固件真实编译通过。当前独立长跑事件/当前状态/轨迹/媒体均为 0。DEMO 的 1 个事件是模拟来源，不计实物成功。

首次长跑 28/30 和第二次 29/30 均真实失败，原件分别保留；退出留连和观察准入随后修复。收尾全量也曾 342 通过、1 失败，发现人工纠正四次取时导致 1 ms 媒体绑定错配；确定性修前测试 2 失败、修后专项 6 通过、最终 345 全量通过。没有删除失败结论或放宽证据门。

## 尚需一次集中物理验收

确认预览来自选定物理镜头；手机显示绑定标记并在真实区域内完成固定 14 轮：静止 5 分钟、同区微调、四次交替移动、遮挡、受控断开重连、六次交替移动。通过条件是 10 次移动至少 8 次正确及 4 个负样本全部通过，同时验证三份媒体、搜索和 MINIMAL 保留。未实际执行前，各物理统计保持未运行，准确率为 null。
