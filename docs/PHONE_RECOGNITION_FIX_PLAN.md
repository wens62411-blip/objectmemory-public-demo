# 手机识别闭环专项修复（2026-09-06）

本轮用户已授权实施。保留现有代码、REAL 数据和参考原图，不重建项目。

## 已复现的失败

- REAL 手机档案 v6 已加载；30 秒 60 个不同源帧没有手机候选，DINO 编码数不增加，当前状态为零。
- 已确认参考原图可解码、哈希一致；原候选器未检出手机。
- NanoDet 接入缺少上游 RGB/等比补边步骤；四组本地对照仍未检出本次参考手机，不能宣称单修前处理就解决。
- 物品/搜索页没有持续更新位置；建档加载状态与现场识别成功混淆。

## 工作顺序与验收

1. 修正候选器输入契约；按固定哈希、上游和许可评估本地候选能力。实际额外评估了 YOLOX 与 YOLOv8n，两者未改善当前参考照片的手机检出，未接入生产；清理本轮未采用权重。保留 NanoDet 并修复其 RGB/等比补边契约，不接受仅返回 list 的测试。
2. 保留类别识别与注册身份两层：未注册手机可以显示候选框，但不能冒充“我的手机”写位置。注册物品仍须真实参考特征与连续观察。
3. 修复上传回执、实时档案状态、页面位置更新和具体失败诊断。低分候选/模型失败不产生确认事件。
4. 真实模型/独立文件素材/真实 FastAPI 与 SQLite 验证；分别报告合成契约测试、真实文件输入与物理摄像头。参考图本身仅作故障回归，不作身份准确率证明。
5. 部署后重测当前摄像头。不能代替用户执行实际拿放；未完成的不同视角/相似物品/10 次拿放如实列出，不再用单元测试数字替代。

## 限制

不新增地图或固件功能；不降低身份真实性门；不外传家庭图像；不自动删除用户照片。关机按文末用户最新授权条件处理。

## 实测后追加的最小修复

DINO 中心裁切会丢失长条物品两端。本轮改为完整物品 fit224 黑边，使用新的预处理版本；原阈值 0.82 和差距门 0.06 不变。用已保存的确认框重建现有档案，原图哈希必须不变。完整结论以 `PHONE_RECOGNITION_FIX_REPORT.md` 和本轮实际结果为准。

## 用户追加：手机外形先验与关机

用户提出利用正面通常为深色长方形的先验。已使用公开实拍三台手机、背景和合成黑矩形/遥控器负例验证轮廓提案。离线 DINO 对部分其他手机仍会给出高相似分，因此最终边界收紧为：轮廓只显示黄色“外形像手机 · 未确认”，不运行身份匹配，不更新位置或事件。真实类别检测加参考身份匹配仍为主链路，不能把轮廓分数说成识别概率。此安全边界不代表已解决用户手机的漏检。

用户最新授权是在本轮测试完成并保存结果后关机（覆盖先前不关机要求）。关机前正常停止本任务摄像头、后端与测试进程，不强制关闭其他未保存程序。关键验收未完成不得当作全部通过后先关机。
